[package]
name = "LuxPassToken"
version = "1.0.0"
authors = []

# Must match ProductPassport and your deploy wallet. Set at publish time, e.g.
#   aptos move publish --named-addresses luxpass=0xYOUR_ADDR
[addresses]
luxpass = "_"

[dependencies.AptosFramework]
git = "https://github.com/aptos-labs/aptos-framework.git"
rev = "devnet"
subdir = "aptos-framework"

[dev-dependencies]
